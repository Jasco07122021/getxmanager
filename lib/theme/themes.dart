import 'package:flutter/material.dart';

class Themes{
  final lightTheme = ThemeData.light().copyWith(canvasColor: Colors.white);
  final darkTheme = ThemeData.dark().copyWith(canvasColor: Colors.white);
}